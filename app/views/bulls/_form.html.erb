<%= form_with(model: bull) do |form| %>
  <% if bull.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(bull.errors.count, "error") %> prohibited this bull from being saved:</h2>

      <ul>
        <% bull.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :born_on, style: "display: block" %>
    <%= form.date_field :born_on %>
  </div>

  <div>
    <%= form.label :offspring_count, style: "display: block" %>
    <%= form.number_field :offspring_count %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
